apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: users-ingress-restricted
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/auth-url: http://authorization.default.svc.cluster.local/verify
    nginx.ingress.kubernetes.io/auth-response-headers: X-JWT-UserId, X-JWT-Role, X-JWT-Username, X-JWT-Email
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
    - host: api.staging.pepeunlimited.com
      http:
        paths:
          - backend:
              serviceName: users
              servicePort: 80
            path: /twirp/pepeunlimited.users.UseService/*
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: users-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: api.staging.pepeunlimited.com
      http:
        paths:
          - backend:
              serviceName: users
              servicePort: 80
            path: /twirp/pepeunlimited.users.UseService/VerifySignIn
          - backend:
              serviceName: users
              servicePort: 80
            path: /twirp/pepeunlimited.users.UseService/CreateUser