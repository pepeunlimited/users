// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"log"
	"time"

	"github.com/facebookincubator/ent/dialect/sql"
)

// dsn for the database. In order to run the tests locally, run the following command:
//
//	 ENT_INTEGRATION_ENDPOINT="root:pass@tcp(localhost:3306)/test?parseTime=True" go test -v
//
var dsn string

func ExampleTicket() {
	if dsn == "" {
		return
	}
	ctx := context.Background()
	drv, err := sql.Open("mysql", dsn)
	if err != nil {
		log.Fatalf("failed creating database client: %v", err)
	}
	defer drv.Close()
	client := NewClient(Driver(drv))
	// creating vertices for the ticket's edges.

	// create ticket vertex with its edges.
	t := client.Ticket.
		Create().
		SetToken("string").
		SetCreatedAt(time.Now()).
		SetExpiresAt(time.Now()).
		SaveX(ctx)
	log.Println("ticket created:", t)

	// query edges.

	// Output:
}
func ExampleUser() {
	if dsn == "" {
		return
	}
	ctx := context.Background()
	drv, err := sql.Open("mysql", dsn)
	if err != nil {
		log.Fatalf("failed creating database client: %v", err)
	}
	defer drv.Close()
	client := NewClient(Driver(drv))
	// creating vertices for the user's edges.
	t0 := client.Ticket.
		Create().
		SetToken("string").
		SetCreatedAt(time.Now()).
		SetExpiresAt(time.Now()).
		SaveX(ctx)
	log.Println("ticket created:", t0)

	// create user vertex with its edges.
	u := client.User.
		Create().
		SetRole("string").
		SetUsername("string").
		SetEmail("string").
		SetPassword("string").
		SetIsDeleted(true).
		SetIsBanned(true).
		SetIsLocked(true).
		SetLastModified(time.Now()).
		AddTickets(t0).
		SaveX(ctx)
	log.Println("user created:", u)

	// query edges.
	t0, err = u.QueryTickets().First(ctx)
	if err != nil {
		log.Fatalf("failed querying tickets: %v", err)
	}
	log.Println("tickets found:", t0)

	// Output:
}
