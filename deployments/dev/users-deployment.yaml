kind: Deployment
apiVersion: apps/v1
metadata:
  name: users
  labels:
    app: users
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - env:
            - name: MYSQL_DATABASE
              value: users
            - name: MYSQL_HOST
              value: external-mysql
            - name: MYSQL_PORT
              value: "3306"
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_USER
              value: root
            - name: RPC_AUTHENTICATION_HOST
              value: http://authentication-twirp.default.svc.cluster.local
            - name: RPC_SPACES_HOST
              value: http://files.default.svc.cluster.local
            - name: SMTP_USERNAME
              value: us3rn4m3
            - name: SMTP_PASSWORD
              value: p4ssw0rd
            - name: SMTP_CLIENT
              value: Mock
          image: pepeunlimited/users:0.1.3.15
          name: users
          ports:
            - containerPort: 8080
          resources: {}
      restartPolicy: Always